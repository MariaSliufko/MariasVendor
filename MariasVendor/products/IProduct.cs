using System;
namespace MariasVendor.products
{
    public interface IProduct //interface with 3 methods. But takes in balance param
    {
        interface IProduct
        {
            void Description();
            int Buy(int balance);
            void Use();
        }
    }
}

